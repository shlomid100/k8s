apiVersion: apps/v1
kind: Deployment
metadata:
  name: cats-app

spec:
  replicas: 2

  selector:
    matchLabels:
      app: cats

  template:
    metadata:
      labels:
        app: cats

    spec:
      containers:
        - name: cats-container
          image: hashicorp/http-echo

          args:
            - "-text=Cats App"

          ports:
            - containerPort: 5678

---
apiVersion: v1
kind: Service
metadata:
  name: cats-service

spec:
  selector:
    app: cats

  ports:
    - port: 80
      targetPort: 5678

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dogs-app

spec:
  replicas: 2

  selector:
    matchLabels:
      app: dogs

  template:
    metadata:
      labels:
        app: dogs

    spec:
      containers:
        - name: dogs-container
          image: hashicorp/http-echo

          args:
            - "-text=Dogs App"

          ports:
            - containerPort: 5678

---
apiVersion: v1
kind: Service
metadata:
  name: dogs-service

spec:
  selector:
    app: dogs

  ports:
    - port: 80
      targetPort: 5678

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: snakes-app

spec:
  replicas: 2

  selector:
    matchLabels:
      app: snakes

  template:
    metadata:
      labels:
        app: snakes

    spec:
      containers:
        - name: snakes-container
          image: hashicorp/http-echo

          args:
            - "-text=Snaks App"

          ports:
            - containerPort: 5678

---
apiVersion: v1
kind: Service
metadata:
  name: snake-service

spec:
  selector:
    app: snakes

  ports:
    - port: 80
      targetPort: 5678


---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: animals-ingress

spec:
  ingressClassName: nginx

  rules:
    - host: animals.local

      http:
        paths:
          - path: /cats
            pathType: Prefix

            backend:
              service:
                name: cats-service

                port:
                  number: 80

          - path: /dogs
            pathType: Prefix

            backend:
              service:
                name: dogs-service

                port:
                  number: 80

          - path: /snakes
            pathType: Prefix

            backend:
              service:
                name: snake-service

                port:
                  number: 80
